export type NavItem = {
  title: string;
  href: string;
};

export type TestimonialType = {
  content: string;
  author: string;
  role?: string;
};

export type ServiceType = {
  title: string;
  description: string;
  icon: string;
};

export type FAQType = {
  question: string;
  answer: string;
};